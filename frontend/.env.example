# Set to 'true' to use mock data (no backend required)
VITE_USE_MOCK=true

# Backend URLs (only needed if VITE_USE_MOCK=false)
VITE_API_URL=http://localhost:8000/api
VITE_WS_URL=ws://localhost:8000/ws
